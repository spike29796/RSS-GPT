# AGENTS.md — 员工手册 v1

本文件规定 AI 编码 Agent 在本仓库中的行为准则。产品细节以 docs/ 为准，本文件不重复。

## 1. 基本行为
- 始终使用中文交流；代码、变量名、API、数据库字段、提交信息用英文
- 动手前先读 docs/ 和现有代码，文档已有答案的不许反问用户
- 我是初学者：解释改动时说明"为什么这么做"，关键决策给出理由，不只是贴代码
- 只改当前任务涉及的内容。发现顺手能优化的地方，记录到 PROG 文档，不擅自改
- 做不到的事直接说做不到，不假装完成

## 2. 技术栈（未经用户同意不得变更）
- 前端：Vue3 + Vite（移动端响应式优先）
- 后端：Node.js 定时采集脚本 + Supabase（数据库/认证）
- 部署：Vercel / 自托管二选一，以 README 记录为准

## 3. 权威文档与优先级
- docs/ 下只维护三类文档：
  - REQ（需求）：新功能前必写，含验收标准
  - PROG（进度）：每次工作结束更新：做了什么、卡在哪、下一步
  - BUG（缺陷）：发现即记，注明关联的 REQ
- 冲突时优先级：用户本次明确要求 &gt; REQ &gt; 现有代码
- 发现代码与文档矛盾时，停下来报告，不静默猜测

## 4. 工作流程
- 每个任务开始前：读 PROG 确认当前阶段和下一步
- 按最小闭环交付：能跑 → 能验证 → 更新 PROG，一次只做一件事
- 数据库改动必须先写迁移脚本，不直接手改库
- "代码能跑"不算完成：PROG 没更新 = 任务没完成

## 5. 范围纪律（防止项目死亡）
- v1 范围冻结在 REQ-v1 中，不在范围内的需求一律回复"已记录，下个版本"
- 单次会议最多实现 2~3 个需求，防止上下文膨胀
- 发现现成开源方案能解决的需求，优先推荐复用而非自研

## 6. Git 纪律
- 未经明确授权不 push、不发布
- 每次提交只含当前任务，不混入无关格式化
- 绝不提交 .env、API key、数据库文件

## 7. 汇报纪律（防止黑箱作业）
- 分步执行：每完成一个子步骤，用一句话汇报"刚完成了什么，下一步做什么"
- 预判长操作：凡是预计超过 1 分钟的操作（安装依赖、下载模型、
  跑构建），开始前必须声明："接下来要做 XX，预计 X 分钟，期间无输出"
  长命令（安装、下载、构建）的输出必须用 tee/Tee-Object 
  同时写入日志文件，文件名说明用途（如 install.log、build.log）
- 维护心跳文件：每完成一步，立刻更新 PROGRESS.md（当前步骤/
  已完成/下一步/遇到的问题），我要随时能打开看进度
- 死亡报告：如果任务失败、超时或被中断，恢复后第一件事是汇报：
  死在哪一步、已完成什么、还剩什么、错误信息原文
- 绝不允许连续 10 分钟没有任何文字输出
- 