# 厂区结构与权限

```
factory(项目文件)/
  头头.md / 员工.md / 测试.md / 厂区结构.md / 既有项目接入.md
                        ← 全员只读，只有包工头能改
  tickets/              ← 头头可写，其余只读（员工仅可写【疑问】【自测结果】栏）
  artifacts/            ← 员工按工单号开文件夹写入；测试岗写 verdict；头头只读
  status.json           ← 各岗位只更新自己负责的行
  ideas.md              ← 全员只读+追加，不许删改别人的点子
  report.md             ← 测试岗追加

src/ （业务代码）        ← 禁区。只有员工持工单、在自己 worktree/分支 内可动
                            头头、测试岗永远禁写；主分支任何人禁直推

自由区：artifacts/T-XXX/ 内部结构员工自定
规则：跨区写入 = 违规，测试岗验收时必查
```

---

## status.json 统一格式

```json
{
  "T-001": {
    "owner": "worker1",
    "stage": "assigned | in_progress | blocked | pending_review | PASS | FAIL",
    "fault": "none | exec_fault | spec_fault",
    "updated": "2026-07-30 11:00",
    "blocker": "none 或一句话说明"
  },
  "heartbeat": {
    "lead": "时间",
    "worker1": "时间",
    "tester": "时间"
  }
}
```

并发纪律：status.json 是单文件无锁。改它前先重新读一遍文件，
只改自己负责的行，立刻写回——慢改会盖掉别人的更新。

## 枚举与标识（全厂统一，不许自造）

- stage：`assigned / in_progress / blocked / pending_review / PASS / FAIL`
- fault：`none / exec_fault / spec_fault`
- 岗位 ID：`lead`（头头）、`worker1/2/3`（员工）、`tester`（测试岗）
- 规则：status.json 和 verdict 里的状态值、归锅值只许用上面的英文枚举，
  中文只许出现在 blocker 说明和证据描述里
