# 厂区结构与权限

**共享区 = `D:\vibe-coding\factory-shared\`**（本仓库父目录下的 factory-shared 目录，绝对路径，仓库外，不进 git，全员实时可见）。
在任何目录（含 worktree）里干活，一律直接用上面的绝对路径，不做相对换算。

```
项目仓库( 当前仓库/ )
  CLAUDE.md
  头头.md / 组长.md / 员工.md / 测试.md / 厂区结构.md / 既有项目接入.md
                        ← 全员只读，只有包工头能改
  docs/                 ← 工单（T-XXX.md）、需求（REQ-*.md）、模块文档（MOD-*.md）
                            跟 git 走。组长、头头仅限在各自分支内可写，
                            落 main 只经头头合并；其余岗位只读
  src/ 等业务代码       ← 禁区。只有员工持工单、在自己 worktree/分支 内可动
                            头头、组长、测试岗永远禁写；主分支任何人禁直推

共享区( D:\vibe-coding\factory-shared\ )
  status/               ← 状态区：一单一个 T-XXX.json，谁持单谁写
                            （头头→组长→员工→测试岗接力）
    heartbeat/          ← 心跳一人一文件（岗位名.json），各写各的，永不撞
  artifacts/            ← 员工按工单号开文件夹写入；测试岗写 verdict；其余只读
  ideas.md              ← 全员只读+追加，不许删改别人的点子
  report.md             ← 测试岗追加

自由区：artifacts/T-XXX/ 内部结构员工自定
规则：跨区写入 = 违规，测试岗验收时必查
```

**窝位 = `D:\vibe-coding\worktrees\`**（仓库平级的集中目录，所有 worktree 一律住这，规矩见下）。

---

## 窝位与销窝（死规矩，不许自创）

- **窝位唯一**：`D:\vibe-coding\worktrees\T-XXX\`，工单号严格 `T-XXX` 三位数字带连字符
- **分支唯一**：员工 `workerN/T-XXX`（N=编号）；组长 `grouplead/T-XXX`。窝和分支同号
- 从 main 最新切出；窝没立好不动手
- **禁在仓库平级目录或任何别处自建项目副本**——发现即验收红线
- **销窝入流程**：测试岗 PASS、头头合并进 main 的**同一动作里**，
  头头 `git worktree remove` + 删分支，不留尾巴（组长的起草窝签发合并时同理）
- FAIL 返工期间窝保留，复验 PASS 后同规则销

## 终端对话框与派工（2026-08-12 头头定；2026-08-13 包工头增补两条）

- **对话框命名 = 岗位名本名**（worker1/worker2/worker3/tester/grouplead），不带任务名等额外后缀；找人靠 `orca terminal list` 现场解 handle，不认旧名牌
- **派新活先清上下文**：给员工吩咐下一件任务前，先向该员工终端发 `/clear`，落定后再发任务简报
- **前提与责任**：清上下文不拖进度的保证 = 组长的工单身区块划分自包含（员工不读全仓库也能照契约施工）；哪张单做不到，记组长头上
- **默认 agent = KimiCode**（命令 `kimi`，已在本机 PATH）：拉起任何岗位对话框一律 `orca terminal create --worktree active --title <岗位名> --command "kimi"`；禁用 claude 等其它 agent，除非包工头当轮点名。**例外：组长默认 claude**（2026-08-13 包工头定）
- **禁从外部创建工作树**：拉起对话只许在现工作树（主仓）新建（`--worktree active`），禁 `orca worktree create`、禁 `--worktree new-child/new-top-level`、禁把岗位终端挂到 Orca 侧另立的工作树。git 层面的窝位规矩（`D:\vibe-coding\worktrees\T-XXX\`）不变，由 shell 命令开，与 Orca 工作树无关

## status/ 状态文件格式

工单状态 `status/T-XXX.json`（一单一个文件，谁持单谁写）：

```json
{
  "owner": "worker1",
  "stage": "drafting | assigned | in_progress | blocked | pending_review | PASS | FAIL",
  "fault": "none | exec_fault | spec_fault",
  "updated": "2026-07-30 11:00",
  "blocker": "none 或一句话说明"
}
```

心跳 `status/heartbeat/<岗位>.json`（一人一文件，只写自己的）：

```json
{"ts": "2026-07-30 11:00"}
```

写纪律：T-XXX.json 同一时间只有当前持单人可写，持单接力为——
`drafting`：头头（工单头）→ 组长（工单身）→ 头头（签发）；
`assigned/in_progress/blocked`：该员工（blocked 后组长可写回复）；
`pending_review` 及之后：测试岗；PASS/FAIL 落定后归头头做终判和合并。
写前先重读、改完立刻写回。

## 枚举与标识（全厂统一，不许自造）

- stage：`drafting / assigned / in_progress / blocked / pending_review / PASS / FAIL`
- fault：`none / exec_fault / spec_fault`
  （spec_fault 须在 verdict 里细分：验收条件错=退头头 / 契约错=退组长）
- 安全级别：`normal / high`（头头立项时标注在工单里，判定规则见 头头.md）
- 岗位 ID：`lead`（头头）、`grouplead`（组长）、`worker1/2/3`（员工）、`tester`（测试岗）
- 组织线：包工头 → 头头 → 组长 → 员工（外包组）；测试岗独立对头头负责
- 规则：status/ 和 verdict 里的状态值、归锅值只许用上面的英文枚举，
  中文只许出现在 blocker 说明和证据描述里
