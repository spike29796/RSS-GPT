# 厂区结构与权限

**共享区 = `D:\vibe-coding\factory-shared\`**（本仓库父目录下的 factory-shared 目录，绝对路径，仓库外，不进 git，全员实时可见）。
在任何目录（含 worktree）里干活，一律直接用上面的绝对路径，不做相对换算。

```
项目仓库( 当前仓库/ )
  CLAUDE.md
  头头.md / 员工.md / 测试.md / 厂区结构.md / 既有项目接入.md
                        ← 全员只读，只有包工头能改
  src/ 等业务代码       ← 禁区。只有员工持工单、在自己 worktree/分支 内可动
                            头头、测试岗永远禁写；主分支任何人禁直推

共享区( D:\vibe-coding\factory-shared\ )
  tickets/              ← 头头可写，其余只读（员工仅可写【疑问】【自测结果】栏）
  artifacts/            ← 员工按工单号开文件夹写入；测试岗写 verdict；头头只读
  status/               ← 状态区：一单一个 T-XXX.json，谁持单谁写（头头→员工→测试岗接力）
    heartbeat/          ← 心跳一人一文件（岗位名.json），各写各的，永不撞
  ideas.md              ← 全员只读+追加，不许删改别人的点子
  report.md             ← 测试岗追加

自由区：artifacts/T-XXX/ 内部结构员工自定
规则：跨区写入 = 违规，测试岗验收时必查
注意：手册里的 tickets/、status/、artifacts/、ideas.md、report.md 一律指共享区里的，
      不再在项目仓库内。
```

---

## status/ 状态文件格式

工单状态 `status/T-XXX.json`（一单一个文件，谁持单谁写）：

```json
{
  "owner": "worker1",
  "stage": "assigned | in_progress | blocked | pending_review | PASS | FAIL",
  "fault": "none | exec_fault | spec_fault",
  "updated": "2026-07-30 11:00",
  "blocker": "none 或一句话说明"
}
```

心跳 `status/heartbeat/<岗位>.json`（一人一文件，只写自己的）：

```json
{"ts": "2026-07-30 11:00"}
```

写纪律：T-XXX.json 同一时间只有当前持单人可写（assigned 前是头头，
施工中是该员工，pending_review 后是测试岗）；写前先重读、改完立刻写回。

## 枚举与标识（全厂统一，不许自造）

- stage：`assigned / in_progress / blocked / pending_review / PASS / FAIL`
- fault：`none / exec_fault / spec_fault`
- 安全级别：`normal / high`（头头派单时标注在工单里，判定规则见 头头.md）
- 岗位 ID：`lead`（头头）、`worker1/2/3`（员工）、`tester`（测试岗）
- 规则：status/ 和 verdict 里的状态值、归锅值只许用上面的英文枚举，
  中文只许出现在 blocker 说明和证据描述里
