# 厂区结构与权限

**共享区 = 仓库父目录下的 `factory-shared\` 目录**（仓库外，不进 git，全员实时可见）。
本厂区当前 = `D:\vibe-coding\factory-shared\`。在任何目录（含 worktree）里干活，一律直接用绝对路径，不做相对换算。

```
项目仓库( 当前仓库/ )
  CLAUDE.md
  头头.md / 组长.md / 员工.md / 测试.md / 厂区结构.md / 派工规程.md / 既有项目接入.md
                        ← 全员只读，只有包工头能改
  docs/                 ← 模块文档（MOD-*.md），跟 git 走。
                            组长仅限自己分支内可写，落 main 只经头头合并；其余岗位只读
  src/ 等业务代码       ← 禁区。只有员工持工单、在自己 worktree/分支 内可动
                            头头、组长、测试岗永远禁写；主分支任何人禁直推

共享区( <仓库父目录>\factory-shared\ )
  status/               ← 状态区：一单一个 T-XXX.json，谁持单谁写
                            （头头→组长→员工→测试岗接力）
    heartbeat/          ← 心跳一人一文件（岗位名.json），各写各的，永不撞
  tickets/              ← 工单（T-XXX.md）住这，不进 git 不 push。谁持单谁写（接力同 status）
  requirements/         ← 需求（REQ-*.md），头头写，其余只读
  docs/                 ← 内部文档（BUG/PROG 与散图），不进 git 不 push
  artifacts/            ← 员工按工单号开文件夹写入；测试岗写 verdict；其余只读
  tools/                ← 厂区工具脚本（board.js 状态板、notify.js 催办调度器）
  board.md              ← 状态板（board.js 自动生成，人人可读，不许手改）
  ideas.md              ← 全员只读+追加，不许删改别人的点子
  report.md             ← 测试岗追加

自由区：artifacts/T-XXX/ 内部结构员工自定
规则：跨区写入 = 违规，测试岗验收时必查
```

## 共享区的设立（开局一次性，通用模板）

共享区开局建一次，之后全员只按手册读写，不自行增删顶层目录。新建厂区时由包工头建（或点名头头代建）。
下面 `<父目录>` 代入该仓库的实际父目录（本厂区 = `D:\vibe-coding`）。

### 设立步骤

```bash
mkdir -p <父目录>/factory-shared/status/heartbeat
mkdir -p <父目录>/factory-shared/tickets
mkdir -p <父目录>/factory-shared/requirements
mkdir -p <父目录>/factory-shared/docs
mkdir -p <父目录>/factory-shared/artifacts
mkdir -p <父目录>/factory-shared/tools
touch <父目录>/factory-shared/ideas.md <父目录>/factory-shared/report.md
```

tools/ 放厂区工具脚本（board.js 状态板、notify.js 催办调度器），board.md 由 board.js 自动生成，不用手建。

### 多项目

一个共享区管一个仓库。同父目录下开第二个项目时，独立建 `factory-shared-<项目名>`（如 `factory-shared-rss-gpt`），
工单编号、状态文件、心跳各自独立，防止撞车。
各项目手册里引用的共享区绝对路径以该项目自己的 CLAUDE.md 为准。

**窝位 = 仓库平级的集中目录**（所有 worktree 一律住这，规矩见下）。
本厂区当前 = `D:\vibe-coding\worktrees\`。

---

## 窝位与销窝（死规矩，不许自创）

- **窝位唯一**：`<窝位目录>\T-XXX\`（本厂区 = `D:\vibe-coding\worktrees\T-XXX\`），工单号严格 `T-XXX` 三位数字带连字符
- **分支唯一**：员工 `workerN/T-XXX`（N=编号）；组长 `grouplead/T-XXX`。窝和分支同号
- 从 main 最新切出；窝没立好不动手
- **禁在仓库平级目录或任何别处自建项目副本**——发现即验收红线
- **销窝入流程**：测试岗 PASS、头头合并进 main 的**同一动作里**，
  头头 `git worktree remove` + 删分支，不留尾巴
- FAIL 返工期间窝保留，复验 PASS 后同规则销

## status/ 状态文件格式

工单状态 `status/T-XXX.json`（一单一个文件，谁持单谁写）：

```json
{
  "owner": "worker1",
  "stage": "drafting | assigned | in_progress | blocked | pending_review | PASS | FAIL",
  "fault": "none | exec_fault | spec_fault",
  "updated": "2026-07-30 11:00",
  "blocker": "none 或一句话说明"
}
```

心跳 `status/heartbeat/<岗位>.json`（一人一文件，只写自己的）：

```json
{"ts": "2026-07-30 11:00"}
```

写纪律：T-XXX.json 同一时间只有当前持单人可写，持单接力为——
`drafting`：头头（工单头）→ 组长（工单身）→ 头头（签发）；
`assigned/in_progress/blocked`：该员工（blocked 后组长可写回复）；
`pending_review` 及之后：测试岗；PASS/FAIL 落定后归头头做终判和合并。
写前先重读、改完立刻写回。

终判退回后 stage 落点（头头终判时写）：
- exec_fault → 退组长盯重做：owner=该员工，stage 写回 in_progress
- spec_fault 契约错 → 退组长补身：owner=grouplead，stage 写回 drafting
- spec_fault 验收条件错 → 退头头改单头：owner=lead，stage 写回 drafting
fault 字段保留记录，到下一轮 verdict 落定再更新

## 枚举与标识（全厂统一，不许自造）

- stage：`drafting / assigned / in_progress / blocked / pending_review / PASS / FAIL`
- fault：`none / exec_fault / spec_fault`
  （spec_fault 须在 verdict 里细分：验收条件错=退头头 / 契约错=退组长）
- 安全级别：`normal / high`（头头立项时标注在工单里，判定规则见 头头.md）
- 岗位 ID：`lead`（头头）、`grouplead`（组长）、`worker1/2/3`（员工）、`tester`（测试岗）
- 组织线：包工头 → 头头 → 组长 → 员工（外包组）；测试岗独立对头头负责
- 规则：status/ 和 verdict 里的状态值、归锅值只许用上面的英文枚举，
  中文只许出现在 blocker 说明和证据描述里

## ideas.md 格式（2026-08-13 包工头定）

条目格式（追加一律新行）：

```
- [待排/冻结/已立项] 一句话点子（提议人 · YYYY-MM-DD）
```

状态标记只有头头可改：立项时改"已立项"并注明工单号，冻结时改"冻结"；
其余人只许追加"待排"行，不许删改别人的点子（原规矩不变）。
