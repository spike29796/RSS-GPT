# 派工规程（终端对话框与员工编制）

**共享区 = `D:\vibe-coding\factory-shared\`**（绝对路径）。本规程两件事：岗位对话框怎么拉起怎么发消息、员工编制怎么定。终端派工动作人人照此执行。

> **命令真名**：本规程里的 `orca` 一律指全路径 `C:\Users\a2977\AppData\Roaming\orca-dev\cli\bin\orca-dev.cmd`（CLI 显示名为 orca，未注册 PATH，调用时写全路径）。

## 一、终端对话框（2026-08-12 头头定；2026-08-13 包工头增补两条）

- **对话框命名 = 岗位名本名**（worker1/worker2/worker3/tester/grouplead），不带任务名等额外后缀；找人靠 `orca terminal list` 现场解 handle，不认旧名牌
- **派新活先清上下文**：给员工吩咐下一件任务前，先向该员工终端发 `/clear`，落定后再发任务简报
- **前提与责任**：清上下文不拖进度的保证 = 组长的工单身区块划分自包含（员工不读全仓库也能照契约施工）；哪张单做不到，记组长头上
- **默认 agent = KimiCode**（命令 `kimi`，已在本机 PATH）：拉起任何岗位对话框一律 `orca terminal create --worktree active --title <岗位名> --command "kimi"`；禁用 claude 等其它 agent，除非包工头当轮点名。**例外：组长默认 claude**（2026-08-13 包工头定）
- **禁从外部创建工作树**：拉起对话只许在现工作树（主仓）新建（`--worktree active`），禁 `orca worktree create`、禁 `--worktree new-child/new-top-level`、禁把岗位终端挂到 Orca 侧另立的工作树。git 层面的窝位规矩（`D:\vibe-coding\worktrees\T-XXX\`）不变，由 shell 命令开，与 Orca 工作树无关
- **拉起后必核验（2026-08-13 包工头增补）**：`orca terminal list` 里该终端必须同时满足——①标题 = 岗位名本名；②所在路径 = 主仓 `D:/vibe-coding/vibe-coding-test`（`--worktree active` 的效果）。凡挂在 `D:/vibe-coding/worktrees/` 下或其他路径的 = 违规从外部拉起的终端，立即 `orca terminal close` 掉重新按本规程拉。核验不过不许开工

### 发消息规矩（对 kimi 终端）

- kimi 的 tip 遮罩会先吃一条：先送一个空回车，再发正文
- 发完 `terminal show` 核验 preview 动起来才算送达

## 二、员工编制（2026-08-13 包工头定）

- **编制权归组长**：拉新员工对话框、派 workerN 编号由组长执行，头头不代劳。
  头头只拉起/管理组长与测试岗对话框
- **拉新判定，三条缺一不可**：
  1. 存在 `assigned` 且无员工的工单（有单没人干）
  2. 现有员工全部 `in_progress`（没有空闲人手）
  3. 编制数不超过并行施工单数（一个员工同一时间只持一单）
- **编号**：workerN 编号递增、不重用、不回收（员工离场编号作废，防历史 owner 混淆）
- **默认编制 3 人**（worker1-3）；要超 3 人，组长向头头报理由，头头认可后组长才拉
- **销窝不归组长**：员工工单终判后由头头销窝删分支（见 厂区结构.md【窝位与销窝】），组长不代劳
